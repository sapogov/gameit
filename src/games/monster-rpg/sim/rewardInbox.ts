import { addItem, requiredAdditionalSlots, type ItemInventory } from './inventory'; import { isItemId, type ItemId } from './items';
export const REWARD_INBOX_LIMIT = 50; export interface RewardBundle { sourceId: string; ownerPlayerId: string; items: readonly { itemId: ItemId; quantity: number }[]; createdAt: string }
export interface RewardInbox { ownerPlayerId: string; bundles: Record<string, RewardBundle> }
export const createRewardInbox = (ownerPlayerId: string): RewardInbox => ({ ownerPlayerId, bundles: {} });
export function enqueueReward(inbox: RewardInbox, bundle: RewardBundle): { ok: true; inbox: RewardInbox } | { ok: false; reason: 'duplicate-source' | 'inbox-full' | 'invalid-bundle' | 'not-owner' } { if (bundle.ownerPlayerId !== inbox.ownerPlayerId) return { ok:false, reason:'not-owner' }; if (inbox.bundles[bundle.sourceId]) return { ok:false, reason:'duplicate-source' }; if (Object.keys(inbox.bundles).length >= REWARD_INBOX_LIMIT) return { ok:false, reason:'inbox-full' }; if (!bundle.items.length || !bundle.items.every((item) => isItemId(item.itemId) && Number.isSafeInteger(item.quantity) && item.quantity > 0)) return {ok:false,reason:'invalid-bundle'}; return {ok:true,inbox:{...inbox,bundles:{...inbox.bundles,[bundle.sourceId]:bundle}}}; }
export function claimReward(inbox: RewardInbox, inventory: ItemInventory, ownerPlayerId: string, sourceId: string): { ok: true; inbox: RewardInbox; inventory: ItemInventory } | { ok: false; reason: 'missing-bundle' | 'not-owner' | 'capacity'; requiredSlots?: number } { if (ownerPlayerId !== inbox.ownerPlayerId) return {ok:false,reason:'not-owner'}; const bundle=inbox.bundles[sourceId]; if(!bundle) return {ok:false,reason:'missing-bundle'}; let preview=inventory; for(const item of bundle.items){const result=addItem(preview,item.itemId,item.quantity); if(!result.ok) return {ok:false,reason:'capacity',requiredSlots: requiredAdditionalSlots(preview,item.itemId,item.quantity)}; preview=result.inventory;} const bundles={...inbox.bundles}; delete bundles[sourceId]; return {ok:true,inventory:preview,inbox:{...inbox,bundles}}; }
