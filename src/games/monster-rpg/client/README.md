# Monster RPG Client

Phaser client code will live here.

Responsibilities:

- Boot Phaser inside the React game host.
- Load assets through stable manifest keys.
- Render maps, characters, creatures, effects, and camera motion.
- Convert keyboard/touch/gamepad input into simulation input actions.
- Display renderer state from simulation/network snapshots without owning source-of-truth rules.

Phase 1 should add the first playable square-tile village slice here.
